name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # The allowlist is FETCHED, never vendored: one source, no copies to drift.
      # Failing here is cheap, and the same check gates the release — so a PR
      # that adds an unapproved or abandoned dependency finds out now.
      - name: Fetch the third-party allowlist
        uses: actions/checkout@v4
        with:
          repository: Particle-Academy/.github
          path: .third-party-allowlist
          persist-credentials: false

      - name: Third-party dependency allowlist
        run: |
          node .third-party-allowlist/third-party/check.mjs --repo .
          node -e "require('node:fs').rmSync('.third-party-allowlist',{recursive:true,force:true})"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install
        run: npm install --no-audit --no-fund --legacy-peer-deps

      - name: Lint
        run: npm run lint

      # NOT `npm test --if-present`. A green tick over an absent suite is worse
      # than a red one, and this package's whole argument is about claims that
      # outrun the code.
      - name: Test
        run: npm test

      - name: Build
        run: npm run build

  php:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ["8.4"]
    steps:
      - uses: actions/checkout@v4

      # A missing toolchain is a FAILURE, not a skip. `skipIf(!HAS_PHP)`
      # returning green is the mechanism that hides two-way drift for months.
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Install
        run: composer install --no-interaction --prefer-dist

      - name: Test
        run: vendor/bin/pest
